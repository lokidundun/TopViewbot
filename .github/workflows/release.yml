name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: false
        type: string

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            arch: x64
            can_test: true
          - os: ubuntu-latest
            platform: linux
            arch: arm64
            can_test: false  # Cross-compiled, cannot test on x64 runner
          - os: macos-latest
            platform: darwin
            arch: arm64
            can_test: true
          - os: windows-latest
            platform: windows
            arch: x64
            can_test: true

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.platform }}-${{ matrix.arch }})

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies (opencode)
        run: |
          cd opencode
          bun install --frozen-lockfile || bun install

      - name: Install dependencies (topviewbot)
        run: |
          cd packages/topviewbot
          bun install --frozen-lockfile || bun install

      - name: Install dependencies (web)
        run: |
          cd web
          bun install --frozen-lockfile || bun install

      - name: Build web frontend
        run: |
          cd web
          bun run build

      - name: Install dependencies (browser-extension)
        run: |
          cd packages/browser-extension
          bun install --frozen-lockfile || bun install

      - name: Build browser extension
        run: |
          cd packages/browser-extension
          bun run build

      - name: Compile and package
        shell: bash
        run: |
          # 从 tag 获取版本号（去掉 v 前缀）
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          export TOPVIEWBOT_VERSION="${TAG#v}"
          chmod +x scripts/*.sh scripts/*.ts 2>/dev/null || true
          # 编译二进制
          bun run scripts/build.ts --platform=${{ matrix.platform }} --arch=${{ matrix.arch }}
          # 打包
          ./scripts/package.sh ${{ matrix.platform }} ${{ matrix.arch }}

      - name: Test startup
        if: matrix.can_test
        shell: bash
        timeout-minutes: 2
        run: |
          chmod +x scripts/test-startup.sh
          ./scripts/test-startup.sh ${{ matrix.platform }} ${{ matrix.arch }} dist/topviewbot-${{ matrix.platform }}-${{ matrix.arch }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: topviewbot-${{ matrix.platform }}-${{ matrix.arch }}
          path: dist/topviewbot-*
          retention-days: 7

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event.inputs.tag

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: |
          echo "Downloaded artifacts:"
          find artifacts -type f -ls

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          name: TopViewbot ${{ github.event.inputs.tag || github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}
          files: |
            artifacts/topviewbot-*/*
          body: |
            ## TopViewbot Release

            ### Downloads

            | Platform | Architecture | Download |
            |----------|--------------|----------|
            | Linux | x64 | `topviewbot-linux-x64-*.tar.gz` |
            | Linux | ARM64 | `topviewbot-linux-arm64-*.tar.gz` |
            | macOS | Apple Silicon | `topviewbot-darwin-arm64-*.tar.gz` |
            | Windows | x64 | `topviewbot-windows-x64-*.zip` |

            ### Installation

            ```bash
            # Linux/macOS
            curl -fsSL https://github.com/${{ github.repository }}/releases/latest/download/topviewbot-linux-x64-${{ github.ref_name }}.tar.gz | tar -xz
            cd topviewbot-linux-x64
            ./topviewbot
            ```

            ### What's Changed

            See the [commits](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }}) for details.
